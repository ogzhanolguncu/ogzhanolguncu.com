import { withLoginRedirect } from '@components/index';
import { AdminLayout } from '@components/Admin';
import React from 'react';

const Posts = () => {
  return (
    <AdminLayout>
      <div>65656</div>
    </AdminLayout>
  );
};

export default withLoginRedirect(Posts);
